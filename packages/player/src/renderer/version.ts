/**
 * Current app version code. Must match versionCode in android/app/build.gradle.
 * Increment this + versionCode in build.gradle when building a new release APK.
 */
export const APP_VERSION_CODE = 4;
